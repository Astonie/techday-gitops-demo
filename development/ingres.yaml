kind: Ingress
metadata:
  name: techday-gitops-demo
  namespace: gitops-demo
spec:
  rules:
  - host: techday-gitops-demo.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: techday-gitops-demo
            port:
              number: 80